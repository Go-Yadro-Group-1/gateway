# Secrets (db.user, db.password) are injected from the environment / .env
# (DB_USER, DB_PASSWORD) and intentionally omitted here. See root .env.example.
db:
  host: postgres
  port: 5432
  dbname: jira_tracker
  sslmode: disable

app:
  log_level: info

metrics:
  port: 9090
