jira:
  baseUrl: "https://issues.apache.org/jira"
  token: ""
  maxResults: 50
  minRetryDelay: 1000
  maxRetryDelay: 60000
  rateLimitPerSec: 5.0

db:
  host: "postgres"
  port: 5432
  user: "pguser"
  password: "pgpassword"
  dbname: "jira_tracker"

app:
  logLevel: "info"
